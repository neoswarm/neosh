<section id="community">
  <div class="glow" aria-hidden="true"></div>
  <div class="wrap center" data-reveal>
    <p class="eyebrow">Community</p>
    <h2>Nobody owns this.</h2>
    <p class="lede">
      neosh is non profit and held by the people who use it.
      No company behind it. Not affiliated with the Neovim team,
      just built in its spirit.
    </p>
    <a class="btn" href="https://github.com/neoswarm/neosh">Join in on GitHub</a>
  </div>
</section>

<style>
  section {
    text-align: center;
  }

  .glow {
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse 50% 60% at 50% 100%, var(--glow-a), transparent 70%);
    pointer-events: none;
  }

  .center {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .lede {
    margin: 0 auto 44px;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    height: 50px;
    padding: 0 26px;
    border-radius: 11px;
    background: var(--fg);
    color: #0a0a0c;
    font-size: 15.5px;
    font-weight: 500;
    transition: opacity 0.2s ease;
  }

  .btn:hover {
    opacity: 0.85;
  }
</style>
